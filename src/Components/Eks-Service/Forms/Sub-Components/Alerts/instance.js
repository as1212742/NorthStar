/** @format */

import { Alert, Container } from "aws-northstar";
import React from "react";

const Alert_Instance = () => {
  return (
    <div style={{ marginBottom: "10px" }}>
      <Alert type="error">Enter all Details</Alert>
    </div>
  );
};

export default Alert_Instance;
