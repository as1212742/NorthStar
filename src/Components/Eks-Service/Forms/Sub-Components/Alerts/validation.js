/** @format */

import { Alert, Container } from "aws-northstar";
import React from "react";

const Validation = () => {
  return (
    <div style={{ marginBottom: "10px" }}>
      <Alert type="error">Enter Correct Details</Alert>
    </div>
  );
};

export default Validation;
